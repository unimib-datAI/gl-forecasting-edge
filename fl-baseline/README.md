# fl-baseline: A Flower / TensorFlow app

## Install dependencies and project

```bash
pip install -e .
```

## Run with the Simulation Engine

In the `fl-baseline` directory, use `flwr run` to run a local simulation:

```bash
flwr run .
```
